<UserControl x:Class="IVPlay.View.ArtView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IVPlay.View"
             xmlns:vm="clr-namespace:IVPlay.ViewModel"  
             
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             x:Name="artView">

    <UserControl.DataContext>
        <vm:ArtViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Image Name="image" Visibility="Hidden" Source="{Binding ElementName=artView, Path=Source}"/>
        <TextBlock Name="text" Visibility="Hidden" Text="{Binding ElementName=artView, Path=Source}" Foreground="{Binding TextForeground, Mode=OneWay}"/>
    </Grid>
</UserControl>
