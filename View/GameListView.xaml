<UserControl x:Class="IV_Play.View.GameListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IV_Play.View"
             xmlns:converter="clr-namespace:IV_Play.WPFWrapper"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.InputBindings>
        
    </UserControl.InputBindings>
    <Grid>
        <ListView Name="gameList" Grid.ColumnSpan="2" Grid.Column="0" Background="Transparent">
            <ListView.InputBindings>
                <KeyBinding Key="Return" />
                <KeyBinding Key="Return" Modifiers="Alt" />
                <KeyBinding Key="Up" />
                <KeyBinding Key="Right" />
            </ListView.InputBindings>
            <ListView.CommandBindings>
                <!--<CommandBinding Command="ComponentCommands.MoveUp" CanExecute="CommandBinding_CanExecute"/>-->
            </ListView.CommandBindings>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <WrapPanel>
                        <Image Name="icon" Source="{Binding icon}" Width="16" Height="16" Margin="0,0,5,0" />
                        <TextBlock Text="{Binding ListText}" Margin="0,0,5,0"/>
                    </WrapPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalAlignment" Value="Left" />
                    <Setter Property="TextBlock.Foreground" Value="White" />
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>
</UserControl>
